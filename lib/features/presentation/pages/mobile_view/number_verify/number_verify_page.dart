import 'package:flutter/material.dart';

class NumberVerifyPage extends StatelessWidget {
  const NumberVerifyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}